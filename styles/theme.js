import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#fff',
        },
        secondary: {
            main: '#000407',
        },
        inherit:{
            main: '#EDB518',
        },
        text: {
            main: '#737b83',
        },
        surface: {
            main: '#4B00D9',
        }
    },
});

export default theme